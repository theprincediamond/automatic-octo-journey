<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Ultracare Hospital</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />
  <style>
    .navbar-custom {
      background-color: #007bff;
    }

    .navbar-custom .nav-link,
    .navbar-custom .navbar-brand {
      color: white;
    }

    .navbar-custom .nav-link:hover {
      color: #ffc107;
    }

    .btn-custom, .btn-outline-custom {
      margin-left: 10px;
      padding: 6px 16px;
      text-decoration: none;
      font-weight: 500;
      display: inline-block;
    }

    .btn-custom {
      background-color: #28a745;
      color: white;
      border: none;
      border-radius: 5px;
    }

    .btn-custom:hover {
      background-color: #218838;
    }

    .btn-outline-custom {
      border: 1px solid #fff;
      color: white;
      border-radius: 5px;
    }

    .btn-outline-custom:hover {
      background-color: white;
      color: #007bff;
    }

    .content {
      padding: 100px 20px;
      text-align: center;
    }

    h2 {
      margin-bottom: 30px;
    }

    footer {
      background-color: #f8f9fa;
      padding: 40px 0;
      text-align: center;
      color: #333;
    }

    .footer-social a {
      margin: 0 10px;
      color: #007bff;
      font-size: 20px;
      transition: color 0.3s ease;
    }

    .footer-social a:hover {
      color: #0056b3;
    }

    .footer-about {
      max-width: 600px;
      margin: 0 auto 20px auto;
    }
  </style>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-custom shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand" asp-controller="Home" asp-action="Index">Ultracare</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav2">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-between" id="navbarNav2">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" asp-controller="Home" asp-action="Doctor">Doctors</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" asp-controller="Home" asp-action="Department">Departments</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" asp-controller="Home" asp-action="Contact">Contact Us</a>
          </li>
        </ul>
        <div class="d-flex">
          @if(User.Identity?.IsAuthenticated == true){
            @if(User.IsInRole("Admin")){
              <a class="btn-outline-custom" asp-controller="Admin" asp-action="Index" target="_blank">Admin Panel</a>
            }
              <a class="btn-outline-custom" asp-controller="Account" asp-action="EditUser">Settings</a>
            @if(User.IsInRole("Patient") || User.IsInRole("Doctor")){
              <a class="btn-outline-custom" asp-controller="Appointment" asp-action="Index">Appointment</a>
            }
              <a class="btn-outline-custom" asp-controller="Account" asp-action="Logout">Logout</a>
          }
          else{
            <a class="btn-outline-custom" asp-controller="Account" asp-action="Login">Login</a>
            <a class="btn-custom" asp-controller="Account" asp-action="Create">Register</a>
          }
        </div>
      </div>
    </div>
  </nav>

  <div class="content">
    @RenderBody()
  </div>

  <footer>
    <div class="footer-about">
      <h5>About Ultracare Hospital</h5>
      <p>Ultracare is committed to providing world-class medical services with compassion and professionalism. Your health is our priority — anytime, anywhere.</p>
    </div>
    <div class="footer-social">
      <a href="https://facebook.com" target="_blank" title="Facebook"><i class="fab fa-facebook"></i></a>
      <a href="https://twitter.com" target="_blank" title="Twitter"><i class="fab fa-twitter"></i></a>
      <a href="https://instagram.com" target="_blank" title="Instagram"><i class="fab fa-instagram"></i></a>
    </div>
    <div class="mt-3">
      <small>&copy; 2025 Ultracare Hospital. All rights reserved.</small>
    </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
